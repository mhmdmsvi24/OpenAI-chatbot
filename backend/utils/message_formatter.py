# def format_message(role, content):
#     """Format user and assistant messages for clarity."""
#     return f"{role.capitalize()}: {content}"


# def format_conversation(history):
#     """Format the full conversation between user and assistant."""
#     formatted = "\n".join(
#         [format_message(entry["user"], entry["assistant"]) for entry in history]
#     )
#     return formatted
